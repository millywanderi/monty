#include "monty.h"

/**
 * nop - a function that does nothing
 * @sta: stack
 * @line_num: line number
 * Return: void
 */
void nop(stack_t **sta, unsigned int line_num)
{
	(void) line_num;
	(void) sta;
}
